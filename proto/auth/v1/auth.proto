syntax = "proto3";

package auth.v1;

service AuthService {
  rpc ValidateTelegramInitData(ValidateTelegramInitDataRequest) returns (ValidateTelegramInitDataResponse) {}
}

message ValidateTelegramInitDataRequest {
  string init_data = 1;
}

message ValidateTelegramInitDataResponse {
  string token = 1;
}
